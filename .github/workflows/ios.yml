name: iOS Build & Test

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: macos-15
    defaults:
      run:
        working-directory: ios

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          XCODE_PATH=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          if [ -z "$XCODE_PATH" ]; then
            echo "No Xcode found"
            exit 1
          fi
          sudo xcode-select -s "$XCODE_PATH"
          xcodebuild -version
          swift --version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode Project
        run: xcodegen generate

      - name: Resolve packages
        run: |
          xcodebuild -resolvePackageDependencies \
            -project Currents.xcodeproj \
            -scheme Currents \
            -clonedSourcePackagesDirPath .spm-cache

      - name: Build
        run: |
          set -o pipefail
          xcodebuild build \
            -project Currents.xcodeproj \
            -scheme Currents \
            -destination 'platform=iOS Simulator,name=iPhone 16 Pro' \
            -derivedDataPath DerivedData \
            -clonedSourcePackagesDirPath .spm-cache \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee build.log

      - name: Test
        run: |
          set -o pipefail
          xcodebuild test \
            -project Currents.xcodeproj \
            -scheme CurrentsTests \
            -destination 'platform=iOS Simulator,name=iPhone 16 Pro' \
            -derivedDataPath DerivedData \
            -clonedSourcePackagesDirPath .spm-cache \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 | tee test.log

      - name: Package .app for Appetize
        run: |
          PRODUCTS_DIR="DerivedData/Build/Products/Debug-iphonesimulator"
          APP_PATH="$PRODUCTS_DIR/Currents.app"

          if [ ! -d "$APP_PATH" ]; then
            echo "Expected .app not at $APP_PATH"
            echo "Searching DerivedData for .app bundles:"
            find DerivedData -name "Currents.app" -type d 2>/dev/null
            exit 1
          fi

          if [ ! -f "$APP_PATH/Currents" ]; then
            echo "No executable binary in .app bundle"
            echo "Contents:"
            ls -la "$APP_PATH/"
            exit 1
          fi

          echo "App binary: $(du -h "$APP_PATH/Currents" | cut -f1)"
          echo "Total .app: $(du -sh "$APP_PATH" | cut -f1)"

          cd "$PRODUCTS_DIR"
          zip -r "$GITHUB_WORKSPACE/Currents-simulator.zip" "Currents.app"

      - name: Upload .app artifact
        uses: actions/upload-artifact@v4
        with:
          name: Currents-simulator
          path: Currents-simulator.zip
          retention-days: 30

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            ios/build.log
            ios/test.log

      - name: Summary
        if: always()
        run: |
          echo "## Build Results" >> $GITHUB_STEP_SUMMARY
          if grep -q "BUILD SUCCEEDED" build.log 2>/dev/null; then
            echo "Build: passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "Build: **FAILED**" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            grep -E "error:" build.log 2>/dev/null | head -20 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
          if grep -q "TEST SUCCEEDED" test.log 2>/dev/null; then
            echo "Tests: passed" >> $GITHUB_STEP_SUMMARY
          elif [ -f test.log ]; then
            echo "Tests: **FAILED**" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Download **Currents-simulator** artifact to test on [Appetize.io](https://appetize.io)" >> $GITHUB_STEP_SUMMARY
